# Salt: Управление конфигурацией на нескольких серверах

## Цель проекта

Настроить централизованное управление конфигурацией проекта через SaltStack (Salt). Salt позволяет автоматизировать настройку и поддержку инфраструктуры на множестве серверов из единой точки управления.

## Зачем нужен Salt?

### Проблема без Salt:
- **Ручная настройка**: Каждый сервер настраивается вручную через SSH
- **Несогласованность**: Разные версии конфигураций на разных серверах
- **Сложность масштабирования**: Добавление нового сервера требует повторения всех шагов
- **Отсутствие контроля версий**: Нет истории изменений конфигураций
- **Сложность аудита**: Трудно отследить, что и когда было изменено

### Решение с Salt:
- **Централизованное управление**: Один Salt Master управляет всеми серверами
- **Идемпотентность**: Применение конфигурации всегда дает одинаковый результат
- **Автоматизация**: Изменения применяются автоматически на всех целевых серверах
- **Версионирование**: Конфигурации хранятся в Git
- **Масштабируемость**: Легко добавить новые серверы - просто установить Salt Minion

## Архитектура проекта

### Инфраструктура на Яндекс.Облаке

Инфраструктура будет развернута в **Яндекс.Облаке** с использованием Terraform. Создаются следующие виртуальные машины:

- **Salt Master** (1 узел) - управляющий сервер Salt
  - Публичный IP для доступа
  - Порты: 4505 (publish), 4506 (ret)
  
- **Nginx серверы** (2-3 узла) - веб-серверы с балансировкой
  - Публичные IP для доступа из интернета
  - Порты: 80, 443
  
- **Backend серверы** (2 узла) - серверы приложений
  - Внутренние IP (доступ через Nginx)
  - Порты: 8000, 8080

Все серверы будут находиться в одной VPC сети `otus-network` и подсети `otus-subnet` в зоне `ru-central1-a`.

### Схема развертывания:

```
Интернет
    ↓
Salt Master (публичный IP)
    ↓ (управление через Salt)
┌─────────────────────────────────┐
│  Яндекс.Облако VPC              │
│  otus-network / otus-subnet     │
│                                 │
│  ┌─────────────┐                │
│  │ Nginx-1     │ (публичный IP) │
│  │ Nginx-2     │ (публичный IP) │
│  └─────────────┘                │
│         ↓                        │
│  ┌─────────────┐                │
│  │ Backend-1   │ (внутренний IP)│
│  │ Backend-2   │ (внутренний IP)│
│  └─────────────┘                │
└─────────────────────────────────┘
```

### Что будем управлять через Salt:

1. **Nginx** - веб-сервер и обратный прокси
   - Балансировка нагрузки между серверами
   - Проксирование запросов к backend сервисам
   - Статический контент
   - SSL/TLS терминация (при необходимости)

2. **iptables** - файрвол для защиты серверов
   - Правила для разрешения необходимых портов
   - Блокировка нежелательного трафика
   - Защита от DDoS атак
   - Логирование подозрительной активности

## План работы

### Этап 0: Подготовка инфраструктуры на Яндекс.Облаке
- [ ] Создание Terraform конфигурации для развертывания ВМ:
  - Salt Master (1 ВМ)
  - Nginx серверы (2-3 ВМ)
  - Backend серверы (2 ВМ)
- [ ] Настройка Security Groups для Яндекс.Облака:
  - Разрешение SSH (22) для всех серверов
  - Разрешение портов Salt (4505, 4506) для Salt Master
  - Разрешение HTTP/HTTPS (80, 443) для Nginx серверов
- [ ] Развертывание инфраструктуры через Terraform
- [ ] Генерация инвентаря серверов

### Этап 1: Развертывание Salt Master
- [ ] Установка Salt Master на управляющем сервере (ВМ в Яндекс.Облаке)
- [ ] Настройка конфигурации Salt Master (`/etc/salt/master`)
- [ ] Настройка Security Group для портов Salt (4505, 4506)
- [ ] Запуск и проверка работы Salt Master
- [ ] Проверка доступности портов

### Этап 2: Установка Salt Minion на целевых серверах
- [ ] Установка Salt Minion на всех серверах через Salt bootstrap:
  - Nginx серверы (2-3 сервера)
  - Backend серверы (2 сервера)
- [ ] Настройка подключения к Salt Master (указание IP Master)
- [ ] Принятие ключей на Salt Master (`salt-key -A`)
- [ ] Проверка связи Master ↔ Minion (`salt '*' test.ping`)

### Этап 3: Создание Salt States для Nginx
- [ ] Создание структуры Salt States
- [ ] State для установки Nginx
- [ ] State для конфигурации Nginx:
  - Базовая конфигурация
  - Виртуальные хосты
  - Upstream серверы для балансировки
  - Логирование
- [ ] State для управления сервисом Nginx
- [ ] Тестирование на тестовом сервере

### Этап 4: Создание Salt States для iptables
- [ ] State для установки и настройки iptables
- [ ] State для правил файрвола:
  - Разрешение SSH (порт 22) - для всех серверов
  - Разрешение портов Salt (4505, 4506) - только для Salt Master
  - Разрешение HTTP/HTTPS (порты 80, 443) - для Nginx серверов
  - Разрешение backend портов (8000, 8080) - только для внутренней сети
  - Разрешение loopback интерфейса
  - Разрешение установленных соединений (ESTABLISHED, RELATED)
  - Блокировка всего остального входящего трафика
  - Логирование заблокированных пакетов
- [ ] State для сохранения правил iptables (`iptables-save`)
- [ ] State для автоматической загрузки правил при перезагрузке
- [ ] Тестирование правил файрвола

### Этап 5: Организация Salt States по ролям
- [ ] Создание ролей (roles) для разных типов серверов:
  - `salt-master` - для Salt Master сервера
  - `nginx` - для Nginx серверов (веб-серверы)
  - `backend` - для Backend серверов (серверы приложений)
- [ ] Настройка pillar для хранения чувствительных данных:
  - IP адреса серверов
  - Конфигурации Nginx
  - Правила файрвола
- [ ] Создание top.sls для распределения состояний по серверам
- [ ] Настройка grains для идентификации ролей серверов

### Этап 6: Тестирование и применение
- [ ] Тестирование состояний в dry-run режиме
- [ ] Применение состояний на тестовом сервере
- [ ] Проверка работоспособности сервисов
- [ ] Применение на всех серверах
- [ ] Документирование процесса применения

## Структура проекта Salt

```
lesson_23/
├── terraform/                     # Terraform конфигурация для Яндекс.Облака
│   ├── main.tf                    # Основная конфигурация ВМ
│   ├── variables.tf               # Переменные Terraform
│   ├── outputs.tf                 # Вывод IP адресов серверов
│   └── security_groups.tf         # Security Groups для Яндекс.Облака
├── salt/                          # Salt States (на Salt Master)
│   ├── top.sls                    # Распределение состояний по серверам
│   ├── nginx/                     # States для Nginx
│   │   ├── init.sls               # Установка и базовая настройка
│   │   ├── config.sls             # Конфигурация Nginx
│   │   └── service.sls            # Управление сервисом
│   ├── iptables/                  # States для iptables
│   │   ├── init.sls               # Установка iptables
│   │   ├── rules.sls              # Правила файрвола
│   │   └── save.sls               # Сохранение правил
│   └── roles/                     # Роли для разных типов серверов
│       ├── salt-master.sls        # Роль для Salt Master
│       ├── nginx.sls              # Роль для Nginx серверов
│       └── backend.sls            # Роль для Backend серверов
├── pillar/                        # Pillar данные (секреты и конфигурации)
│   ├── top.sls                    # Распределение pillar данных
│   ├── nginx.sls                  # Конфигурация Nginx
│   └── iptables.sls               # Правила файрвола
├── scripts/                       # Вспомогательные скрипты
│   ├── deploy-infra.sh           # Развертывание инфраструктуры
│   ├── install-salt-master.sh    # Установка Salt Master
│   └── install-salt-minion.sh    # Установка Salt Minion
├── README.md                      # Этот файл
└── APPLY.md                       # Инструкция по применению состояний
```

## Преимущества использования Salt

1. **Автоматизация**: Один раз настроили - применяется везде
2. **Консистентность**: Все серверы имеют одинаковую конфигурацию
3. **Быстрое развертывание**: Новый сервер настраивается за минуты
4. **Откат изменений**: Легко вернуться к предыдущей версии конфигурации
5. **Мониторинг**: Salt может проверять состояние серверов
6. **Масштабируемость**: Легко добавить сотни серверов
7. **Веб-интерфейс**: SaltGUI предоставляет удобный графический интерфейс для управления

## SaltGUI - Веб-интерфейс для Salt

В проекте установлен **SaltGUI** - open-source веб-интерфейс для управления Salt.

### Возможности SaltGUI:
- 📊 Просмотр всех minion серверов
- ⚡ Выполнение команд на серверах
- 🔧 Применение Salt States через веб-интерфейс
- 📝 Просмотр истории выполнения команд
- 🔑 Управление ключами minion
- 📈 Мониторинг состояния серверов

### Доступ к SaltGUI:
```
http://<salt-master-ip>:8000
```

Подробнее см. [SALT_UI.md](SALT_UI.md)

## Требования

- Аккаунт в Яндекс.Облаке
- Установленный `yc` CLI (Yandex Cloud CLI)
- Установленный Terraform >= 0.13
- SSH ключ для доступа к серверам (`~/.ssh/id_ed25519.pub`)
- Существующая VPC сеть `otus-network` и подсеть `otus-subnet` в Яндекс.Облаке

## Быстрый старт

### 1. Подготовка окружения

```bash
# Экспорт переменных Яндекс.Облака
export YC_TOKEN=$(yc iam create-token)
export YC_CLOUD_ID=$(yc config get cloud-id)
export YC_FOLDER_ID=$(yc config get folder-id)

# Переход в директорию проекта
cd /home/sedunovsv/OTUS/OTUS_HI-2025-01/lesson_23
```

### 2. Развертывание инфраструктуры

```bash
# Автоматическое развертывание (рекомендуется)
./start.sh

# Или вручную
cd terraform
terraform init
terraform apply
cd ../ansible
ansible-playbook playbooks/site.yml
```

### 3. Доступ к SaltGUI

После развертывания SaltGUI доступен по адресу:

```
http://<salt-master-ip>:8000
```

Например: `http://130.193.51.250:8000`

### 4. Применение Salt States

```bash
# Через Ansible
cd ansible
ansible-playbook playbooks/apply-salt-states.yml

# Или через Salt Master
ssh ubuntu@<salt-master-ip>
sudo salt '*' state.apply

# Или через SaltGUI веб-интерфейс
# Откройте http://<salt-master-ip>:8000 и используйте интерфейс
```

## Следующие шаги

После выполнения этого ДЗ можно расширить использование Salt для:
- Управления конфигурациями баз данных
- Управления конфигурациями балансировщиков
- Автоматического развертывания приложений
- Мониторинга состояния серверов
- Автоматического обновления системы
- Управления SSL сертификатами

## Дополнительные ресурсы

- [Документация SaltStack](https://docs.saltproject.io/)
- [Salt States Tutorial](https://docs.saltproject.io/en/latest/topics/tutorials/states_pt1.html)
- [Salt Pillar](https://docs.saltproject.io/en/latest/topics/pillar/index.html)

